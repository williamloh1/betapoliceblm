install.packages("‘gtrendsR’")
install.packages("gtrendsR")
?gtrendsR
?"gtrendsR"
gtrendsR
library(gtrendsR)
data("categories")
str(categories)
View(categories)
data(USstateAbbreviations)
data(USstateAbbreviations)
data('USstateAbbreviations')
data('state')
str('state')
str(state)
str(state.abb)
View(state.x77)
View(state.center)
View(state.x77)
########  Package:  ‘gtrendsR’
####
rm(list = ls())
library(gtrendsR)
#---  Categories dataset composed of exisiting research categories (not vital)
data("categories")
str(categories)
data('state')
state.abb
state.df <- as.data.frame(cbind(state.x77, state.abb, state.area, state.division, state.name, state.region))
rm(list=setdiff(ls(), "state.df", "categories"))
rm(list=setdiff(ls(), c("state.df", "categories")))
library(gtrendsR)
########  Package:  ‘gtrendsR’
####
rm(list = ls())
library(gtrendsR)
#---  Categories dataset composed of exisiting research categories (not vital)
data("categories")
#---  Categories dataset composed of exisiting research categories (not vital)
data("categories")
str(categories)
data('state')
state.df <- as.data.frame(cbind(state.x77, state.abb, state.area, state.division, state.name, state.region))
rm(list=setdiff(ls(), c("state.df", "categories")))
gtrends(c("NHL", "NFL"), geo = c("CA", "US"), category = 20)
my.fun <- function(vector.keyword = NA,  state.abbr = "US", ){
gtrends(keyword = c("firearms", "gun"), geo = as.character(state), time = "today 3-m",
gprop = c("web"),
category = 0, hl = "en-US", low_search_volume = FALSE,
cookie_url = "http://trends.google.com/Cookies/NID", tz = 0,
onlyInterest = FALSE)
}
my.fun <- function(vector.keyword = NA,  state.abbr = "US"){
gtrends(keyword = c("firearms", "gun"), geo = as.character(state), time = "today 3-m",
gprop = c("web"),
category = 0, hl = "en-US", low_search_volume = FALSE,
cookie_url = "http://trends.google.com/Cookies/NID", tz = 0,
onlyInterest = FALSE)
}
for(i in state.df$state.abb){
my.fun(vector.keyword = c("gun", "firearms"), state = i)
}
state.abbr
my.fun(vector.keyword = c("gun", "firearms"), state.abbr = "CA")
my.fun(vector.keyword = c("gun", "firearms"), state.abbr = "CA")
my.fun <- function(vector.keyword = NA,  state.abbr = "US"){
gtrends(keyword = c("firearms", "gun"), geo = as.character(state.abbr), time = "today 3-m",
gprop = c("web"),
category = 0, hl = "en-US", low_search_volume = FALSE,
cookie_url = "http://trends.google.com/Cookies/NID", tz = 0,
onlyInterest = FALSE)
}
my.fun(vector.keyword = c("gun", "firearms"), state.abbr = "CA")
#---  Example:
my.fun(vector.keyword = c("gun", "firearms"), state.abbr = "CA")
#---  For all states:
list.gun.firearms <- list(NULL)
